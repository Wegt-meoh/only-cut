import { html, css, LitElement } from 'lit';
import { customElement } from 'lit/decorators.js';

@customElement('control-board')
export class ControlBoard extends LitElement {
    static styles = css`
       
    `;

    render() {
        return html`
        
        `
    }
}